# 2026.241.0957 — chore: migrate charly.yml schema to 2026.240.1943 (version bump only)

## Summary

The new spec (v0.2026241.931) requires every charly.yml at schema 2026.240.1943. This is a version-bump-only change — no structural edits.

## How tested

```
$ grep -rn "2026.232.0520" --include="*.yml" . | grep -v ".git" | head -3
(empty — no remaining old-schema files)

$ grep -m1 "^version:" charly.yml
version: 2026.240.1943
```

**Schema validation (A2)** — the migrated charly.yml is accepted by the new schema (charly binary built from the committed tree with spec v0.2026241.931):

```
$ cd /tmp/migrate-debian && CHARLY_DEPLOY_CONFIG=/tmp/validate-debian.yml charly box validate
EXIT: 0
```

## Change classification

- **Change class:** config (schema version stamp)
- **Verification gate:** grep verification + `charly box validate` against the new schema (EXIT 0)
- **Attribution tier:** `fully tested and validated` — the migrated config is accepted by the new schema (validated live).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*