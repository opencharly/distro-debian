# 2026.256.1123 — docs: correct debian box README

## Summary

Correct stale facts in `README.md` — the debian box README. The requested
`box/debian/README.md` does not exist in this repo; the box README is the
repo-root `README.md`, which is where every described stale fact lives. No prose
was added.

- Candy refs moved off `opencharly/charly/candy/*` into standalone repos; the
  README now cites the real
  `@github.com/opencharly/<layer-*|pod-*|plugin-*>[:subdir]:<tag>` shape pinned
  to explicit CalVer tags, not "a single tag".
- The disposable bed is rebuilt with the canonical
  `charly -C box/debian check run check-debian-debootstrap-vm`, not
  `charly update`.
- The schema-version hard-fail message was corrected to match the live source.

## How tested

Documentation-only change — no runtime gate. Every correction was verified
against the live tree.

```
$ grep -n "charly -C box/debian" README.md
48:charly -C box/debian box build debian
63:libvirt/QEMU and carries `disposable: true`, so `charly -C box/debian check run

$ sed -n "36,39p" charly.yml   # the bed is a check entity
check-debian-debootstrap-vm:
    vm:
        from: debian-debootstrap
        disposable: true

$ grep -rhoE "@github.com/opencharly/(layer|pod|plugin)-[A-Za-z0-9._/-]+:[vV][A-Za-z0-9._-]+" charly.yml box/*/charly.yml | sort -u | head -4
@github.com/opencharly/layer-agent-forwarding:v2026.239.1607
@github.com/opencharly/layer-asciinema:v2026.240.0119
@github.com/opencharly/layer-build-toolchain:v2026.239.1636
@github.com/opencharly/layer-charly:v2026.241.1407

$ grep -rhoE "@github.com/opencharly/(layer|pod|plugin)-[A-Za-z0-9._/-]+:[vV][0-9][A-Za-z0-9._-]+" charly.yml box/*/charly.yml | sed "s/.*://" | sort -u | wc -l   # distinct tags
23
```

## Rulebook compliance

- **R1** (`/charly-internals:root-cause-analyzer`): each divergence was
  root-caused to the candy de-submodule cutover / the check-verb cutover before
  rewriting it. No "pre-existing" classification.
- **R2** (`/charly-internals:skills`): the claim-keyed sweep greps the whole
  repo — `charly/candy/` and `single tag` now return only this fixed surface.
- **R5** (`/charly-internals:skills`): the stale ref shape and the superseded
  `charly update` bed command are removed completely.
- **R6** (`/charly-internals:git-workflow`): edits made in a fresh
  `/tmp/opencode/distro-debian-refs` worktree of the submodule, never in the
  umbrella checkout; PR-only landing, no force-push, no direct push to `main`.

## Change classification

documentation-only → `documentation reviewed` (the whole diff is markdown; no
pins or scripts changed). Materially the debian README only.

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*